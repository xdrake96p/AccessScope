# Release plugin IDE v1.0.4

Fix: risoluzione `AccessScope-cli.jar` su Android Studio (PluginManager + fallback embedded).

| File | Uso |
|------|-----|
| `AccessScope-1.0.4.zip` | Android Studio → *Settings → Plugins → Install from Disk* |
