# Release plugin IDE v1.0.0

Artefatti pronti per i colleghi (inclusi anche nella GitHub Release dell'app).

| File | Uso |
|------|-----|
| `access-scope-1.0.0.vsix` | VS Code / Cursor → *Extensions → Install from VSIX* |
| `android-studio-plugin-1.0.0.zip` | Android Studio → *Settings → Plugins → Install from Disk* |
| `access-scope-cli.jar` | CLI standalone (`java -jar access-scope-cli.jar devices list`) |

L'APK dell'app AccessScope viene pubblicato separatamente nella GitHub Release `v1.3.0` (non in questa cartella perché è in `.gitignore`).
